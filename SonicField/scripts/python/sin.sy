import random
import math
from java.lang import System
for y in range(1,5):
    t1=System.nanoTime()
    
    sig=sf.MakeTriangle(sf.SineWave(1000,1000))
    
    for x in range(1,2):
        sig=sf.DirtyPower(sig,2)

    sig=sf.FixSize(sig)
    sig=sf.Realise(sig)
    
    t2=System.nanoTime()
    print float(t2-t1)/1000000.0
    sf.WriteFile32((+sig,sig),"temp/dirty-10-sin.wav")